Kubernetes is a container orchestrator, and the team moved scheduling onto it.

The migration also brought in Nomad, a batch scheduler, for periodic work.

Later the team added Consul (a service registry) to the same cluster.

The Kubernetes cluster grew to forty nodes, and Nomad kept the batch queue short.
